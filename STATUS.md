# STATUS — GNSS Defense Monitor

Updated as of build verification on 2026-05-09. Use this as the running ledger of what's real vs mocked.

## What's done — verified live

- **ML inference layer (`ml/inference.py`)** — `score()` for `texbat`, `aissou`, `opensky_ensemble`. Smoke test passes (`python -m ml.inference --all`).
- **Backend** — FastAPI on port 8000 with all required routes:
  - `GET /api/health` — model versions + per-scenario inference latency. Verified.
  - `GET /api/scenarios` — 5 scenarios. Verified.
  - `WS /ws/onboard?scenario=…` — 100ms ticks with L1+L2 scoring. Verified.
  - `WS /ws/globe?scenario=…` — 1500ms batches, ensemble per-aircraft. Verified.
  - `POST /api/inject/{scenario}` + `/reset` — flips replay cursor; verdict transitions within 1 tick. Verified.
  - `GET /api/explain/{tick_id}` — honest 200 + `status: "pending"`, no fake SHAP. Verified.
  - `GET /api/report/{session_id}` — real PDF via reportlab. Verified (3.3 KB, valid PDF header).
- **Backend pytest suite** — 16 passing tests covering all new endpoints + injection demo + WS schemas.
- **Frontend** — Next.js 16 + React 19 + Tailwind v4 + Leaflet:
  - Top bar with mode switcher, scenario picker (mode-aware), live INFERENCE latency badge, INJECT button (red flash on click), EXPORT REPORT.
  - OnboardMonitor: callsign / position / heading card, twin score bars (L1, L2) with threshold/critical markers + 60s sparkline, top-reasons strip in Polish, live alert feed.
  - LiveGlobe: dark Carto tiles via react-leaflet, color-coded markers, sortable aircraft list, ensemble breakdown panel showing iforest_v1 / iforest_v2 / lstm_ae sub-ratios.
  - ExplainModal: opens on Explain button, shows the PENDING placeholder honestly.
  - ModelFooter: live model versions + F1 strip pulled from `/api/health`.
  - Mock fallback: client-side feed when backend unreachable; amber banner.
- **End-to-end Playwright sweep** — 5 scenarios screenshotted, inject flow verified before/after, aircraft selection on globe verified. Outputs in `e2e_screenshots/`.
- **Production build** — `npm run build` succeeds (Next.js static page route).

## What's mocked vs real

| Component                | Real?                                                                                      |
| ------------------------ | ------------------------------------------------------------------------------------------ |
| Pipeline                 | **Real.** joblib + torch artifacts loaded from `models/` on first call.                    |
| Models themselves        | **Synthetic.** Trained on procedural data (fixed seed) to stand in for the real datasets.  |
| Feature schemas          | **Real shape.** TEXBAT 27d, Aissou 80d (12×8), OpenSky 16d + traj 16×5.                    |
| Replay scenarios         | **Synthetic CSVs.** Generated by `scripts/generate_scenarios.py`, attack patterns realistic. |
| Inject mechanism         | **Real.** Mutates a per-scenario flag; verdict provably climbs in next ticks.              |
| PDF report               | **Real.** Real reportlab output with model versions, verdict timeline, latency self-check. |
| `/api/explain` SHAP      | **Honest stub.** Returns 200 with `status: "pending"`; no fake values.                     |
| Frontend mock fallback   | **Real.** Client-side feed runs if backend goes down mid-demo.                             |

## Per-layer F1 (claimed in spec; not measured)

- L1 (TEXBAT XGBoost):  0.984
- L2 (Aissou XGBoost):  0.976
- L3 iforest-v1:         0.789
- L3 iforest-v2:         0.743
- L3 LSTM-AE:            0.935
- L3 ensemble (OR-fusion): 0.935

Synthetic data over-fits trivially (smoke test shows F1 ≈ 1.0). The footer reports the spec numbers, which is intentional and documented in DECISIONS.md (D2).

## Known limitations / what's not done

- LSTM-AE on the synthetic dataset produces extremely high reconstruction errors on out-of-distribution inputs (teleport scenarios) — the score is clipped at `4.0×` for display. Real models would be better calibrated; this is mentioned in the demo notes.
- No real OpenSky / AISStream feed wired into the new GNSS endpoints (existing legacy `/api/flights/live` still hits OpenSky if creds are set, but the GNSS Defense Monitor scenarios use deterministic CSVs by design).
- SHAP TreeExplainer integration is a placeholder (per spec — must not fake values).
- No persistence: sessions are in-memory. Restarting uvicorn clears the inject state.
- The INFERENCE latency badge in the top bar reflects most-recent tick latency. The full per-scenario latency profile is in `/api/health.inference_latency_ms`.

## URLs after `make demo`

- Frontend dashboard:   http://localhost:3000
- Backend OpenAPI docs: http://localhost:8000/docs
- Health probe:         http://localhost:8000/api/health
- Scenarios list:       http://localhost:8000/api/scenarios

## Start commands

```bash
# One-shot from a clean clone:
make install    # pip + npm + nvm/Node 20 (if absent)
make demo       # uvicorn + next dev, both auto-restart on change

# Manual:
GPS_SENTINEL_MODELS=$PWD/models PYTHONPATH=$PWD:$PWD/backend \
  backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000

cd frontend && npm run dev   # requires Node 20 (nvm use 20)
```
