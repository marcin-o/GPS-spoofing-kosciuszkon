fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
websockets==14.1
joblib==1.4.2
numpy==2.2.0
pandas==2.2.3
scikit-learn==1.6.0
xgboost==2.1.3
shap==0.46.0

# L3 LSTM-AE in the OpenSky ensemble (CPU-only).
# Install with: pip install --index-url https://download.pytorch.org/whl/cpu torch==2.11.0
torch==2.11.0
# PDF report generation for /api/report/{session_id}.
reportlab==4.2.5

# Test deps
pytest==8.3.4

# Optional: piper-tts (offline neural TTS for /api/alerts/{id}/speak).
# If unavailable on the demo laptop the route falls back to the pre-recorded
# MP3s under app/data/voice_backup/. Install with: pip install piper-tts
# piper-tts==1.2.0
